# Model Notes

Create one note per model. Record equations, variables, units, assumptions, calibration, initial conditions, numerical method, validation tests, known failure modes, and links to reproducible artifacts.

