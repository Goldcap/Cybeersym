# Artifacts

Store generated figures, datasets, simulation outputs, notebooks, and exports here. Each artifact should link back to the model, conversation, or review that produced it and include enough metadata to reproduce it.

